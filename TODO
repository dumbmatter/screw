normal controls (stop/pause/seek)
- pause: node.disconnect, see usage already, probably move that to pause function
- stop: (same as for selecting new file) need to reevaluate, since start is not used stop can't be

queue

export mp3
